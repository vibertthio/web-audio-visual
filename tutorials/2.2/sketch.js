function setup() {
  createCanvas(800, 500);
}

function draw() {
  // comment out this list
  // see what happends
  // background(100);

  stroke(255);
  fill(0);
  ellipse(mouseX, mouseY, 50, 50);
}

// Event
function touchMoved() {
  // stroke(255);
  // fill(0);
  // ellipse(mouseX, mouseY, 50, 50);
}
